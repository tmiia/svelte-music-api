import { tv } from 'tailwind-variants';

export const styles = tv({
  slots: {
    btn: 'p-3 pl-3.5 flex items-center justify-center opacity-85 hover:scale-90 transition-transform duration-300'
  }
});
